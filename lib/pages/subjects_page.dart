import 'package:flutter/material.dart';
import '../models/dummyData.dart';

class SubjectsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("SubjectName"),
      ),
      body: Text("Subject Page"),
    );
  }
}
